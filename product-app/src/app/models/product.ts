export class Product {
    // public available : boolean;
    // public rating : number;
    constructor(public code:string, public name:string, public price:number,
        public available:boolean, public rating : number){
    }
}
